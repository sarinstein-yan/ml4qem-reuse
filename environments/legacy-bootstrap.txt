# ML4QEM paper-era reproduction environment
#
# Python is pinned separately to CPython 3.9.25. Installation is deliberately
# staged because the upstream requirements do not pin torch and torch-sparse's
# build imports torch. This file records the extra bootstrap constraints; it is
# not a replacement for the unmodified upstream requirements.txt.
# `code/bootstrap_legacy_environment.sh` uses `LEGACY_PYTHON` when supplied,
# then searches for `python3.9`, and finally asks `uv` for its managed 3.9
# interpreter.

setuptools<81
wheel==0.45.1
numpy==1.24.4
torch==2.1.2+cpu  # install from https://download.pytorch.org/whl/cpu
torch-scatter==2.1.2  # wheel from https://data.pyg.org/whl/torch-2.1.0+cpu.html
torch-sparse==0.6.18  # wheel from https://data.pyg.org/whl/torch-2.1.0+cpu.html
scikit-learn==1.3.2  # imported by both public demos but absent from requirements.txt
